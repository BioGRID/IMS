
/**
 * Dataset Javascript Bindings that apply
 * to the dataset specific pages of the site.
 */
 
;(function( yourcode ) {

	yourcode( window.jQuery, window, document );

} (function( $, window, document ) {

	$(function( ) {
		$("#section-curation").curationWorkflow( { } );
	});

}));